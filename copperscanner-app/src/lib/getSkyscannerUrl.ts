export function getSkyscannerUrl(origin: string, destination: string) {
    // You may need to map city names to Skyscanner codes here
    // For demo, just encode the names
    return `https://www.skyscanner.net`;
  }